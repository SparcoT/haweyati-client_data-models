name: haweyati_client_data_models
description: A simple command-line application.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  hive: 1.4.4+1
  meta: ^1.2.3
  hive_flutter:
  json_serializable:
  shared_preferences:
  google_maps_flutter:
  firebase_dynamic_links:
  http:
  dio:
  fluttertoast:

  flutter:
    sdk: flutter

dev_dependencies:
  build_runner:
  hive_generator:
  json_serializable_generator:
